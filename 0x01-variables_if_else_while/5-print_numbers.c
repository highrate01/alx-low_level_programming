#include <stdio.h>
/**
 * main - The entry point of the program
 *
 * Return: always 0(success)
 *
 */
int main(void)
{
int a;

for (a = 0; a < 10; a++)
	printf("%d", a);
		putchar('\n');
		return (0);
}
